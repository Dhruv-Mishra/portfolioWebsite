{
  "name": "dhruvwebsite-workspace",
  "private": true,
  "packageManager": "bun@1.4.0",
  "scripts": {
    "dev": "bun run --cwd portfolio dev",
    "build": "bun run --cwd portfolio build",
    "start": "bun run --cwd portfolio start",
    "lint": "bun run --cwd portfolio lint",
    "typecheck": "bun run --cwd portfolio typecheck"
  }
}
